# Minimal dependencies for the headless pipeline (backend/run_pipeline.py).
# Engines 02–04 only: no Playwright, no Chromium, no spaCy. Used by GitHub Actions.
httpx>=0.27.0
ddgs>=6.1.0
google-genai>=1.20.0
groq>=0.9.0
python-dotenv>=1.0.0
aiosqlite>=0.20.0
